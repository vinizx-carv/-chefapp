import 'package:flutter/material.dart';

class TelaFavorito extends StatelessWidget {
  const TelaFavorito({super.key});

  @override
  Widget build(BuildContext context) {

    return const Center(
      child: Text('Tela Favorito'),
    );
  }
}